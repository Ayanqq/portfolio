import React from 'react';
import {Icon} from "./icon/Icon";

type ContactCardPropsType = {
    subCard:Array<string>;
    leftText:string;
    rightText:string;
}
export const ContactCard = (props:ContactCardPropsType) => {
    return (
        <div>

        </div>
    );
};
